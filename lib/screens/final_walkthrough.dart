import 'package:flutter/material.dart';

class FinalWalkthroughScreen extends StatelessWidget {
  const FinalWalkthroughScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: SafeArea(
        child: Center(
          child: Text("Hello!"),
        ),
      ),
    );
  }
}
